define(['models/progress-calculator'], function (ProgressCalculator) {
  describe('ProgressCalculator', function () {
    it('exists', function () {
      expect(ProgressCalculator).to.be.ok;
    });
  });
});